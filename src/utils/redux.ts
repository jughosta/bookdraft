export enum LoadingStatus {
  initial = 'initial',
  loading = 'loading',
  loaded = 'loaded',
  failed = 'failed',
}
export enum UpdatingStatus {
  initial = 'initial',
  processing = 'processing',
  succeeded = 'succeeded',
  failed = 'failed',
}
